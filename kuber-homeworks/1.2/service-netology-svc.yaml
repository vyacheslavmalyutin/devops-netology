apiVersion: v1
kind: Service
metadata:
  name: netology-svc
spec:
  selector:
    app: pod-netology
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443